{
  "name": "chat-frontend",
  "version": "1.0.0",
  "description": "Frontend para chat",
  "main": "index.js",
  "scripts": {
    "dev": "rm -rf ./development && rm -rf ./.cache && parcel public/index.html --out-dir development -p 3000",
    "build": "parcel build public/index.html --out-dir dist --public-url ./",
    "replit": "rm -rf ./development && rm -rf ./.cache && parcel public/index.html --out-dir replit -p 80"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xsrpm/chat-frontend.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/xsrpm/chat-frontend/issues"
  },
  "homepage": "https://github.com/xsrpm/chat-frontend#readme",
  "devDependencies": {
    "parcel-bundler": "1.12.3"
  }
}
